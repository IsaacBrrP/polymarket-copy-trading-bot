# Polymarket Copy Trading Bot - Environment Configuration

# ─── Required ──────────────────────────────────────────────────────────────────

# Comma-separated list of Ethereum addresses to copy trade
# Example: USER_ADDRESSES='0xabc123...,0xdef456...'
USER_ADDRESSES=

# Your Polygon wallet address (proxy wallet / operator wallet)
# Example: PROXY_WALLET='0x1234567890abcdef...'
PROXY_WALLET=

# Your wallet private key (without 0x prefix)
# SECURITY WARNING: Never commit this to version control!
# Example: PRIVATE_KEY='abcdef1234567890...'
PRIVATE_KEY=

# Polygon RPC endpoint URL (HTTPS required)
# Example: RPC_URL='https://polygon-mainnet.infura.io/v3/YOUR_PROJECT_ID'
RPC_URL=

# ─── Allowance & Readiness ─────────────────────────────────────────────────────

# Polymarket CTF Exchange spender address on Polygon (default below is correct for mainnet)
# Only change if Polymarket updates their contract.
POLYMARKET_SPENDER=0x4bFb41d5B3570DeFd03C39a9A4d8de6bd8B8982E

# Minimum USDC balance/allowance threshold for readiness check (default: 10)
MIN_USDC_THRESHOLD=10

# Minimum native POL/MATIC balance for gas (default: 0.01)
MIN_GAS_THRESHOLD=0.01

# Set to 'false' to run even with readiness warnings (default: strict mode)
READINESS_STRICT=true

# ─── Monitoring / Status Server ────────────────────────────────────────────────

# Port for the local status HTTP server (dashboard at http://localhost:3001/)
STATUS_PORT=3001

# Set to 'false' to disable the status server
STATUS_ENABLED=true

# ─── Optional / Tuning ─────────────────────────────────────────────────────────

# MongoDB connection string (optional, for trade history)
# Example: MONGO_URI='mongodb://localhost:27017/polymarket-bot'
MONGO_URI=

# Fetch interval in seconds (how often to check for new trades)
# Default: 1
FETCH_INTERVAL=1

# Trade multiplier (scale your position size relative to tracked traders)
# Example: 1.0 = same size, 2.0 = double size, 0.5 = half size
# Default: 1.0
TRADE_MULTIPLIER=1.0

# Maximum retry attempts for failed operations
# Default: 3
RETRY_LIMIT=3

# Enable trade aggregation (combine multiple small trades into one)
# Default: false
TRADE_AGGREGATION_ENABLED=false

# Time window for trade aggregation in seconds
# Default: 300 (5 minutes)
TRADE_AGGREGATION_WINDOW_SECONDS=300

